# 📦 Public Algorithm Presets Directory

## Responsibility
This directory stores pre-calculated static execution trace JSON files for standard algorithms (e.g., Bubble Sort, QuickSort, Binary Search, Tree Traversals).

## Features & Usage
- Allows instantaneous playback of popular algorithms without waiting for dynamic AST trace generation.
- Static files served directly by Vite's public asset server.
